const Wiki = () => (
  <div style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    minHeight: '80vh',
    textAlign: 'center',
  }}>
    <div className="glass" style={{ padding: '48px 64px' }}>
      <h2 style={{ marginBottom: 12 }}>知识库</h2>
      <p style={{ color: 'var(--text-secondary)' }}>建设中...</p>
    </div>
  </div>
);

export default Wiki;
